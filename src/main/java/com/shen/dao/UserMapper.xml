<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.shen.dao.UserMapper">

    <!-- 增加一个用户 -->
    <insert id="addUser" parameterType="com.shen.pojo.Users">
        INSERT INTO usertable (username, password, registration_username, created_at)
        VALUES (#{username}, #{password}, #{registrationUsername}, #{createdAt})
    </insert>

    <!-- 删除一个用户 -->
    <delete id="deleteUser" parameterType="int">
        DELETE FROM usertable WHERE id = #{id}
    </delete>

    <!-- 更新一个用户 -->
    <update id="updateUser" parameterType="com.shen.pojo.Users">
        UPDATE usertable
        SET username = #{username},
        password = #{password},
        registration_username = #{registrationUsername},
        created_at = #{createdAt}
        WHERE id = #{id}
    </update>

    <!-- 查询一个用户 -->
    <select id="getUserById" parameterType="int" resultType="com.shen.pojo.Users">
        SELECT id, username, password, registration_username, created_at
        FROM usertable
        WHERE id = #{id}
    </select>

    <!-- 查询所有用户 -->
    <select id="getAllUsers" resultType="com.shen.pojo.Users">
        SELECT id, username, password, registration_username, created_at
        FROM usertable
    </select>

</mapper>
